# UI TOPIC INSTRUCTIONS

- Build from reusable components and explicit state models.
- Include accessibility and deterministic behavior checks.
- Put framework-specific implementations in:
  - `topics/ui/frameworks/streamlit/`
  - `topics/ui/frameworks/gradio/`
- Apply all core standards.
