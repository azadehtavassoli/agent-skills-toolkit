# RAG TOPIC INSTRUCTIONS

- Use shared deterministic pipeline components under `topics/rag/shared/`.
- Keep framework-specific implementations under `topics/rag/frameworks/`.
- Enforce groundedness, citation checks, and structured evaluation artifacts.
- Apply all core standards.
