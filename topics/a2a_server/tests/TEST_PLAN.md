# A2A Server Topic Test Plan

- Validate Agent Card schema/content and skill metadata.
- Validate deterministic executor responses for fixed request inputs.
- Validate request handler wiring with task store behavior.
- Validate streaming and non-streaming response paths.
- Validate explicit cancellation/error behavior (no silent fallbacks).
