# Scenario Authoring Checklist

Before adding a new demo scenario, confirm:
- goal is explicit
- audience is explicit
- core message is explicit
- preconditions and seed state are defined
- steps are deterministic
- hero moment is clear
- output profiles are defined
- selectors are stable
- scenario validates against schema
- scenario can run end-to-end
